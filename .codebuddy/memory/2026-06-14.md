# 2026-06-14

## 工作记录
- 诊断并修复 `daily-update.yml` GitHub Actions 报错：`python -m scripts.fetch_rules` 需要 `scripts/` 是 Python 包但缺 `__init__.py`
- 创建 `scripts/__init__.py`（空文件）
- 移除 `daily-update.yml` 中 fetch 步骤的 `continue-on-error: true`，避免隐藏真实错误
- 第二次修复：merge_rules.py 中 `generate_source_list` 直接访问 `source['timestamp']`，但 disabled 状态的源没有 timestamp 字段 → KeyError。改为用 `.get()` 安全访问并对不同状态分别处理
- 文档更新：将 PROJECT_DOCS.md、README.md、README_EN.md 中所有 `sources.json` 引用更新为 `sources.txt`，并添加 txt 格式的配置指引（格式: 名称 > 订阅地址，行首 # 禁用）。代码已支持 txt 格式（fetch_rules.py 读取 `config/sources.txt`），文档现已同步。
