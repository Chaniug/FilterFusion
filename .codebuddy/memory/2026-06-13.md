# 2026-06-13

- 全面审查了 FilterFusion 项目（广告过滤规则聚合工具），生成了完整的项目文档 `PROJECT_DOCS.md`，涵盖架构、数据流、组件详解、使用方法、部署订阅、技术细节等。
- 优化了 GitHub Release 命名格式：Tag 从 `release-YYYY.MM.DD` 改为简洁的 `YYYY.MM.DD`，Release 标题从"每周规则集发布·稳定版"改为 `FilterFusion Weekly · YYYY.MM.DD`，修改了 `.github/workflows/weekly-release.yml`。
- 修复了 PowerShell 环境问题：WinGet CommandNotFound 预测器导致 `PipelineStoppedException`，通过禁用该模块并在 `$PROFILE` 中添加 `Remove-Module` 指令永久修复。
- 配置了 GitHub Release 自动 Verified 徽章：在 `weekly-release.yml` 中添加 `permissions: contents: write`、`fetch-depth: 0`，并通过 GitHub Actions 自动创建 git tag，利用 GitHub 内置验证使 Release 显示 Verified。
- 为项目设计了 4 套 AI 海报生成方案（赛博朋克风格、技术蓝图风格等）。
- 取消了 logging 和 tests 两个开发任务。
- 修复了 checksum 生成方式：将 `scripts/merge_rules.py` 中的 checksum 算法从 SHA-256（64字符十六进制）改为 ABP 标准的 MD5 + Base64（24字符），完全兼容 uBlock Origin、AdGuard 等广告拦截器。同时更新了 `PROJECT_DOCS.md` 中的相关说明。
- 优化了 `config/default.header` 模板：新增 `[Adblock Plus 2.0]` 首行声明、移除冗余的 `Updated` 和 `Format` 行、扩展 Description 说明兼容性。将硬编码在 `merge_rules.py` 中的规则统计行（Combined/Total/Repetitions）改为模板占位符，统一管理。
- 进一步优化模板实现双格式兼容（ABP + AdGuard）：新增 `! TimeUpdated:` ISO 8601 字段，`! Checksum:` 移至首行之后对齐 AdGuard 规范，简化 Description。修复 header 模板末尾无换行导致与规则粘连的问题，在代码中使用 `rstrip("\n") + "\n"` 确保恰好一个换行。
- 升级 GitHub Actions：`actions/setup-python@v4` → `@v5`，Python `3.10` → `3.12`。
- 修复 GitHub Actions 工作流问题：1) `daily-update.yml` 最终版：彻底放弃 rebase，改为「Actions 本地产出优先」——直接 `git reset --soft origin/main` + `git checkout origin/main -- ':!dist'` 以远程为基础但保留本地生成的 dist/，4 行命令零分支逻辑，永不报错；2) 修复 `weekly-release.yml`，在创建 Tag 前添加 `git pull origin main` 拉取最新代码，确保基于最新 commit 打标签；3) 验证了两个 YAML 文件的语法正确性。
- 全项目 Bug 审查与修复（6项）：
  1. `merge_rules.py`：修复 `##` 元素隐藏规则被误判为注释而丢弃的严重 bug，改为精确判断——`!` 开头为注释，`#` 后跟 `#@%$?` 为规则指令，孤立 `#` 才是注释；
  2. `merge_rules.py`：`calculate_source_stats` 补上 `errors='replace'`，与 `collect_and_process_rules` 对齐，防止非 UTF-8 文件崩溃；
  3. `fetch_rules.py`：超时从 `10+attempt*5`（15-25s）改为 `20+attempt*15`（35-65s），适配 AdGuard Chinese 等大型规则文件；
  4. `config/sources.json`：修复第 4 个源 `Adguard Extra` 的缩进为 4 空格；
  5. `weekly-release.yml`：LINE_COUNT 从 `wc -l`（含注释）改为 `grep -vcE` 仅统计实际规则行；
  6. `merge_rules.py`：新增 `element_hide` 分类（元素隐藏规则），输出中独立成段。
- 修复 `daily-update.yml` 实际运行报错及最终简化：rebase 前 dist/ 有 unstaged changes 导致 `cannot rebase` + `no rebase in progress`。先加了 stage + abort 容错修复，但最终讨论确认 — GitHub Actions 产出的 dist/ 就是真理，不需要 rebase。终极方案：`git fetch → reset --soft origin/main → checkout origin/main -- ':!dist' → git add dist`，4 行，永不冲突。
- 升级 GitHub Actions 消除 Node.js 20 弃用警告：`actions/checkout@v4`→`@v6`，`actions/setup-python@v5`→`@v6`（两个 workflow 文件均已更新，v6 原生支持 Node.js 24）
- 为 `weekly-release.yml` 添加 `workflow_dispatch` inputs（`release_type`: regular/major, `release_notes`），支持手动触发重大版本发布
- 手动触发 v2.0 重大更新 Release (release_type=major)，包含完整更新亮点公告
- Python 3.10+ 激进升级完成：
  1. 修复 `merge_rules.py` 缩进问题、删除 `fetch_rules.py` 未使用的 `Optional` 导入
  2. 更新 `requirements.txt`：`requests>=2.31.0`
  3. 为 `fetch_rules.py` 和 `merge_rules.py` 添加完整类型注解
  4. 创建 `pyproject.toml`（项目配置 + mypy/basedpyright/pytest/black/isort）
  5. 创建 `requirements-dev.txt`
  6. 更新文档（README.md、README_EN.md、PROJECT_DOCS.md）Python 版本 3.8→3.10
  7. basedpyright 验证：0 errors, 0 warnings, 0 notes
  8. 运行合并脚本验证：占位符全部正确替换，adblock-main.txt 复制正常
  9. GitHub Actions 兼容性验证通过
