# 2026-06-18 工作日志

## FilterFusion 第二轮优化（Python 3.13 升级 + httpx 异步 + CI 加速）

完成 5 个任务：
1. 配置升级：pyproject.toml `requires-python>=3.13`，依赖 `requests` → `httpx[http2]>=0.27.0`，工具链 target 3.13
2. fetch_rules.py 重写：`httpx.AsyncClient` + `asyncio.gather` + HTTP/2，`FetchStatus(StrEnum)`、`datetime.UTC`、`__slots__`、`type` 语句，修复所有 encoding
3. merge_rules.py 优化：`_SPECIAL_RE`/`_REGEX_CHAR_RE` 类级预编译正则替代每次调用重建列表，`format_map` + `_SafeDict` 替代 11 次链式 replace，单次 `datetime.now(UTC)`，`RuleType(StrEnum)`，`__slots__`
4. 工作流优化：3 个 workflow Python 3.13 + `uv pip install`，daily-update 合并 step + find 简化清理，weekly-release 加 concurrency，static.yml 加 paths 过滤
5. 文档同步：README/README_EN/PROJECT_DOCS Python 版本与依赖，PROJECT_LOG 追加第二轮日志

输出产物位置与格式完全不变。本地 lint 错误为环境问题（无 Python 3.13/httpx），CI 中正常。

## CI 修复：uv pip install --system 错误

`daily-update.yml` 中 `uv pip install --system` 报错 "No system Python installation found for Python 3.13"。原因是 `setup-uv@v6` 安装的 Python 由 uv 管理而非系统级。修复：
- 去掉 `--system` 标志
- `python -m scripts.xxx` 改为 `uv run python -m scripts.xxx`

## CI 修复：完整迭代过程（4 轮报错 → 最终修复）

GPU Actions CI 由于最新优化（Python 3.13 + uv）触发连环报错，经多轮迭代全部修复：

1. **`--system` 报错**: `uv pip install --system` → 无系统 Python 3.13 → 改为 `uv venv && uv pip install -r requirements.txt`
2. **`uv sync` 构建失败**: setuptools 发现多个顶层目录（rules/assets/config）→ 不构建项目，仅安装依赖
3. **`uv run` 构建失败**: `uv run` 仍尝试构建项目 → 加 `--no-project` 跳过构建
4. **pyproject.toml 修复**: `license = {text = "MIT"}` → `license = "MIT"`（消除 SetuptoolsDeprecationWarning）；新增 `[tool.setuptools.packages.find]` 配置 `include = ["scripts*"]`，仅将 scripts 识别为 Python 包
5. **Node 20 弃用警告**: `astral-sh/setup-uv@v6` → `@v8.2.0`（v8.2.0 使用 node24；v8 短标签不存在需用完整版本号）

最终 daily-update.yml 依赖安装流程：`uv venv` → `uv pip install -r requirements.txt` → `uv run --no-project python -m scripts.xxx`

## 本地 Python 环境确认为 3.14

用户本地使用 Python 3.14。`requires-python = ">=3.13"` 已覆盖，无需修改版本约束。更新了以下文件以标注兼容性：
- `pyproject.toml`: black `target-version` 添加 `'py314'`
- `README.md` / `README_EN.md` / `PROJECT_DOCS.md`: 补充 "本地开发可使用 3.14" 说明
- `MEMORY.md`: 记录本地环境 3.14

## CI Python 从 3.13 升级到 3.14

确认 GitHub Actions 官方 `actions/python-versions` 已支持 Python 3.14.6。`daily-update.yml` 中 `python-version: '3.13'` → `'3.14'`，本地与 CI 环境统一。

## 添加 DNS 过滤规则支持

为 FilterFusion 项目添加 DNS 过滤规则的自动合并功能：

1. **新增配置文件**：
   - `config/dns_sources.txt`：DNS 规则源配置（AdGuard DNS、HaGeZi DNS）
   - `config/dns.header`：DNS 规则头部模板（不含 `[Adblock Plus 2.0]` 和 `{CHECKSUM}`）

2. **新增脚本**：
   - `scripts/fetch_dns_rules.py`：DNS 规则抓取脚本（复用 `fetch_rules.py` 的异步抓取逻辑）
   - `scripts/merge_dns_rules.py`：DNS 规则合并脚本（简化去重合并，不需要复杂分类）

3. **更新 CI 工作流**：
   - `.github/workflows/daily-update.yml`：添加 DNS 规则抓取和合并步骤

4. **更新文档**：
   - `README.md` / `README_EN.md` / `PROJECT_DOCS.md`：添加 DNS 规则说明和订阅地址

5. **测试验证**：
   - 本地运行 `fetch_dns_rules.py` 和 `merge_dns_rules.py` 成功
   - 生成 `dist/dns-blocklist.txt`（157,541 条规则）
   - 所有更改已提交并推送到远程仓库

**注意**：Windows PowerShell 控制台编码为 GBK，脚本中的 emoji 字符会导致 `UnicodeEncodeError`，已移除所有 emoji 字符。

## 历史归档策略调整：3天 → 1天

鉴于 DNS 规则引入后 `dist/` 目录历史文件数量翻倍（AdBlock 3个 + DNS 3个 = 6个历史文件），将历史文件保留从 3 天缩减到 1 天：

- `.github/workflows/daily-update.yml`：`-mtime +2` → `-mtime +0`，step 名称更新
- `PROJECT_DOCS.md`：4 处文档同步更新（目录说明、AdBlock 归档、DNS 归档、清理策略）
- `docs/PROJECT_LOG.md`：追加 DNS 支持说明 + 归档策略变更记录

稳定后 `dist/` 仅保留 6 个文件（2 main + 2 历史 + 2 JSON）。需更早版本通过 git 历史获取。

## 项目目录整理

根据用户反馈"dist 里出现 JSON 文件体感不好"，完成目录归拢整理：

- **删除 `rules/`**：`fetch_meta.json` 和 `dns_fetch_meta.json` 移入 `scripts/`
- **净化 `dist/`**：`summary.json` 和 `dns_summary.json` 移入 `config/`，`dist/` 只留纯规则 `.txt`
- **根目录归位**：`_cdnauth.txt` → `config/`，`PROJECT_DOCS.md` → `docs/`
- **同步修改**：4 个脚本的 `rules_dir` → `scripts`、summary JSON 路径 → `config/`；`.gitignore` 移除 `rules/` 规则；`daily-update.yml` git checkout/add 覆盖 `config` 和 `scripts`
- **文档更新**：`PROJECT_DOCS.md` 全量路径更新（上一轮遗漏 6 处在本轮修复）；`PROJECT_LOG.md` 追加目录整理日志；`merge_optimization_plan.md` 修正 `summary.json` 路径
- **最终结构**：5 个业务文件夹（assets/config/dist/docs/scripts），净减少 1 个；全局搜索零残留

确认 README.md 和 README_EN.md 无路径引用，无需修改。

## PROJECT_DOCS.md 添加 Mermaid 图表

为提升文档可读性，将 ASCII 艺术图全面升级为 Mermaid 图表（GitHub 原生渲染）：

1. **Section 2 数据流图**：两套 ASCII 图合并为一张 Mermaid flowchart，左右并排展示 AdBlock + DNS 流水线
2. **Section 2 四阶段流水线**：ASCII → Mermaid flowchart，四色标注四个阶段
3. **Section 5.2 CI/CD 工作流**：新增 Mermaid 图，展示 3 个 workflow 的触发条件和执行动作
4. **Section 9 去重算法**：ASCII → Mermaid flowchart，展示 isascii 快速路径 + NFKC 回退 + 集合去重
5. **Section 9 规则分类决策树**：新增 Mermaid 图，按 7 级优先级展示分类判断逻辑

文档版本号从 1.3 升级到 1.4。所有 Mermaid 块闭合验证通过。

## README.md 高级感升级

将 README.md 从 522 行模板化风格重写为高级感设计：

1. **Hero 区域**：新增 tagline "多源广告过滤规则的自动聚合引擎"，badge 从 11 个精简到 5 个核心
2. **Why FilterFusion 对比表**：关于 section 插入 6 维度对比表（手动维护 vs FilterFusion）
3. **Mermaid 图表**：工作原理 ASCII 箭头 → 双管道并行流程图 + 应用场景 4 个 ASCII 框 → 四色场景图
4. **使用指南精简**：4 步缩为 4 个加粗标题段落，去除冗余说明
5. **FAQ 精简**：10 条 → 6 条，Q6+Q7 合并入 Q3，删除 Q9/Q10
6. **统计归拢**：贡献者统计 + activity graph + 5 个参与度 badge 统一归到「如何参与」section
7. **视觉降噪**：`---` 分隔线 13→6 条，标题 emoji 削减，目录锚点同步更新
8. **许可证精简**：从 12 行缩减到 2 行

## README_EN.md 同步升级

将英文版 README_EN.md 同步为与中文版完全一致的高级感设计：
- 结构、内容、Mermaid 图表、FAQ 精简、统计归拢、视觉降噪 — 全部对齐
- 2 个 Mermaid 块 + 6 条 `---` 分隔线，与中文版严格一致
- README_JP.md 尚未创建（中文版标注"敬请期待"），无需同步

## 日语/韩语 README 创建 + 语言切换链接更新

- 创建 `README_JP.md`：完整日语翻译，与中文版结构/内容/Mermaid 完全一致
- 创建 `README_KO.md`：完整韩语翻译，与中文版结构/内容/Mermaid 完全一致
- 更新 `README.md` 和 `README_EN.md` 语言切换行：移除"敬请期待"，添加 4 语言平铺链接
- 4 个 README 均验证通过：各 2 个 Mermaid 块 + 6 条分隔线

## README_JP.md 新增「日本の友人への手紙」章节

在日语 README 的贡献方法与许可证之间新增政治声明章节：台湾は中国の領土の神聖にして不可侵の一部です。どうか中国の内政に干渉しないでください。
