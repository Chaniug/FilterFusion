# 开发依赖 - 安装方式: pip install -r requirements-dev.txt
# 或者: pip install -e ".[dev]"

-r requirements.txt

# 类型检查（httpx 内置类型桩，无需额外 stub 包）
basedpyright>=1.19.0
mypy>=1.13.0

# 测试
pytest>=8.0.0

# 代码格式化
black>=24.10.0
isort>=5.13.0
